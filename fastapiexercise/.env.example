# .env.example

# Veritabanı Bağlantı Bilgileri (Yerel Geliştirme İçin)
# Bu değerler, uygulamanızı Docker Compose olmadan doğrudan çalıştırdığınızda kullanılır.
# Docker Compose ile çalışırken, docker-compose.yaml dosyasındaki ortam değişkenleri önceliklidir.
DB_NAME=denemedb
DB_USER=postgres
DB_PASSWORD=PASSWORD # Buraya kendi yerel PostgreSQL şifrenizi girin
DB_HOST=localhost
DB_PORT=5432

# Uygulama Konfigürasyon Yolu (İsteğe Bağlı)
# Eğer uygulamanızın config.toml dosyasını belirli bir yoldan okumasını istiyorsanız ayarlayın.
# Docker ortamında genellikle /app/config.toml olarak ayarlanır.
# Yerel çalışmada bu satırı yorum satırı yapabilir veya boş bırakabilirsiniz.
# CONFIG_PATH=/path/to/your/config.toml
